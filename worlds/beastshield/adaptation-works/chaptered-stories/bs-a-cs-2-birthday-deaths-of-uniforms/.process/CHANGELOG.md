# CHANGELOG — bs-a-cs-2-birthday-deaths-of-uniforms

## 2026-05-07 — 目录规范化补充

**变更原因**：Beastshield 示范重构（Phase 5）补漏。该作品目录已符合编码规范，但缺少 `metadata.yaml` 和 `.process/` 结构。

**变更内容**：
- 创建 `.process/` 目录结构
- 创建 `metadata.yaml`
- `tmp/` 目录保留（已被 `.gitignore` 忽略）
