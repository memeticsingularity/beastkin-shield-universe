# CHANGELOG — eks-o-cm-001-recall-protocol

## 2026-05-07 — 目录规范化补充

**变更原因**：Beastkin Universe 规范重构（Phase 6）。该作品缺少 `metadata.yaml` 和 `.process/` 结构。

**变更内容**：
- 创建 `.process/` 目录结构
- 创建 `metadata.yaml`
- 章节文件迁入 `chapters/` 目录
